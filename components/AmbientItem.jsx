import { StyleSheet, View, Text } from "react-native";

const AmbientItem = ({data}) => {

    return (
        <View>

        </View>
    );
};

const styles = StyleSheet.create({
    item: {

    }
});